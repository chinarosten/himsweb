/**
 * @author rosten
 * @created 2013-11-27
 * control version
 */
define([
	"dojo/_base/window",
	], function(win, kernel, lang, xhr, domConstruct, dom, has, domClass) {
	
	return kernel;
});
